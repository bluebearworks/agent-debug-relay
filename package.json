{
  "name": "agent-debug-relay-monorepo",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces --if-present build",
    "build:extension": "npm run build -w packages/extension",
    "package:extension": "npm run package -w packages/extension",
    "pack:cli": "npm pack -w packages/cli --dry-run"
  },
  "devDependencies": {}
}
