# Agent Debug Relay Skill

The installable Agent Debug Relay skill lives at `.agents/skills/agent-debug-relay/SKILL.md`.

Install it with:

```powershell
npx skills add bluebearworks/agent-debug-relay -g -a codex claude-code opencode -s agent-debug-relay -y --copy --full-depth
```
